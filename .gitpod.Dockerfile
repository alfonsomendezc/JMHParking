# .gitpod.Dockerfile — keep it simple; inherit Gitpod’s base image
FROM gitpod/workspace-full:latest
# (Optional) global tools or OS packages you rely on:
# RUN sudo apt-get update && sudo apt-get install -y <package> && sudo rm -rf /var/lib/apt/lists/*
